services:
  backend:
    ports:
      - "8000:8000"

  frontend:
    ports:
      - "3000:3000"
    build:
      context: ./frontend_next-1
      args:
        NEXT_PUBLIC_BACKEND_URL: http://localhost:8000

  mlflow:
    ports:
      - "5000:5000"

  tunnel:
    profiles:
      - production
